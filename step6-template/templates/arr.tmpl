<!DOCTYPE html>
<html>
<head>
    <title>{{.title}}</title>
</head>
<body>
    <h1>{{.title}}</h1>
    <ul>
        {{range .stuArr}}
        <li>{{.Name}} - {{.Age}} years old</li>
        {{end}}
    </ul>
</body>
</html>